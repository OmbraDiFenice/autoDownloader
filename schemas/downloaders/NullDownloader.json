{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://example.com/autoDownloader/schemas/downloaders/NullDownloader.json",
  "title": "Null downloader",
  "description": "Fake downloader that doesn't do anything. Useful in case you're defining an item just to fetch URLs that are then handled via external scripts",
  "type": "object",
  "required": [
    "type"
  ],
  "properties": {
    "type": {
      "type": "string",
      "enum": [
        "NullDownloader"
      ]
    }
  }
}